{
  "buildCommand": "cd packages/db && DATABASE_URL=$DATABASE_URL npx prisma generate && cd ../.. && pnpm turbo build --filter=@maeum/web...",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
