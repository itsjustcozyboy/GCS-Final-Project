{
  "buildCommand": "npx prisma generate --schema=packages/db/prisma/schema.prisma && pnpm turbo build --filter=@maeum/web...",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
