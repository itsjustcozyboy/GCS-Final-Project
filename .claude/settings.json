{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); if [ -z \"$file\" ]; then exit 0; fi; cd /workspaces/GCS-Final-Project && git add \"$file\" && git diff --cached --quiet && exit 0; git commit -m \"auto: $(basename $file) 변경 저장\" && git push 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "변경사항 커밋 중..."
          }
        ]
      }
    ]
  }
}
